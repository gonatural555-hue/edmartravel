"use client";

import CinematicExperienceScene, {
  type CinematicExperienceSceneProps,
} from "./CinematicExperienceScene";

export default function CityWorldScene(props: CinematicExperienceSceneProps) {
  return <CinematicExperienceScene {...props} />;
}
